# 논문 계획 — CAV 2027 Short Application Papers

> **개정 요약 (2026-07-27, rev.3).** rev.2에서 바뀐 것:
>
> 1. **§2 novelty 주장이 문헌 확인으로 반증됐다 — 전면 재작성.** Isabelle/HOL `function`은
>    겹치는 절에 대해 **pattern compatibility 증명 의무**(겹치는 패턴 쌍마다 우변이 같을
>    것)를 실제로 생성한다. HOL Light도 같다. rev.2 §2.2의 "ITP 계열은 합류성을 검사하지
>    않는다"는 **거짓**이다. 새 차별점은 §2.2의 **다섯 축**(조건의 자유변수 / 비생성자 헤드
>    패턴 / 비좌선형 좌변 / order-sorted / 결정절차·규모)이다.
> 2. **§1·§5 수치를 tip `eda41eed`로 전면 갱신.** 합류성 163→**169**, TIMEOUT 7→**13**,
>    미측정 402→**396**. "TIMEOUT 7건 전부 비트 조작"은 이제 거짓 — 874~885규칙 6건이
>    새로 TIMEOUT이다.
> 3. **비용 비예측성 증거 교체.** 계획서가 안 쓰던 더 좋은 데이터가 표에 이미 있다 —
>    **동일 202규칙** `$bin_band` 142.9초 vs `$bin_bor` 1,792.7초(12.5배). 규칙 수를
>    통제한 대조라 "규칙 수가 비용을 예측하지 않는다"의 결정적 증거다.
> 4. **SCC(실험 ③)는 미착수가 아니라 1/3 완료.** `subty_`/`match_` 951심볼 전수 스윕이
>    끝났고 **진짜 누락(`dom:narrow`) 0건 · 503심볼 COMPLETE**다. 발견 축의 성격이
>    "결함 사냥"에서 "**전체성의 기계 증명 + 근사 부채의 정량화**"로 바뀐다.
> 5. rev.2에서 이어지는 것: 규모 주장 축별 분리, 비용 특성화의 기여 승격, `== false` 근사
>    정량화(실험 ⑨) 최우선, 증분 게이트 비용(실험 ⑩), 2단계 리뷰 대응 배치, 고정 예산 +
>    층화 표본 프로토콜, 검사기 이름 최소화.
>
> 측정 수치의 원본은 [verification.md](verification.md), 방법·이력은
> [verification-notes.md](verification-notes.md), 실험 태스크는
> [spectec/lib/rewrite/todo.md](spectec/lib/rewrite/todo.md).
> 이 파일은 **왜 그렇게 쓰는지**를 관리한다.

---

## 0. 포고 목표 — CFP 중심 확인부 (2026 CFP 기준, 2027년 동일 가정)

- **트랙**: Short Application Papers. 10p max, **참고문헌·부록 제외**, LNCS,
  **double-blind 필수**(위반 시 리뷰 없이 리젝트). 2026 유의, "academic work, as
  opposed to industrial experience reports"가 명시돼 있다.
- **루브릭 직역**: "기존 알고리즘/정리의 실제 적용, 또는 기존 이론 결과의 확장"을
  요구하고, **해결하는 정리적 문제의 중요성**, 간략한 related work, 구별되는 특징,
  적절한 비교를 분명히 할 것. "very early stage work은 부적합."
  → **§1이 "적용한 정리"를 이름으로 호출해야 한다**(아래 §5).
  → **rev.3 주의**: novelty가 좁아진 것은 이 트랙에서 **불리하지 않다.** 루브릭이 요구하는
    것은 "새 이론"이 아니라 "기존 정리의 실제 적용 + 문제의 중요성"이다. §2의 재작성은
    주장을 약화시키는 대신 **트랙 적합도를 올린다.**
- **⚠️ 2단계 리뷰(2026 유의, 계획에 반드시 반영)**: 1라운드는 리뷰 **2개**, 지지를
  못 얻으면 **리버틀 없이 조기 리젝트**. 1라운드 통보는 마감 약 5주 후.
  → **지면 제약**: 페이지 1에 수치 결과가 있어야 한다. "동기 → 배경 → 기여 불릿" 순서는
  조기 리젝트 구조다.
- **부록**: 허용되되 "리뷰어가 읽을 의무 없음"이 CFP 명문.
  → **건전성 라거나 본문, 표 원본·재현 절차는 부록.** 역으로 배치하면 안 된다.
- **artifact**: 최종 수락이 AE에 조건부가 아니고, 제출 시점엔 **제출 의향만 표시**.
  의향을 표시하고 안 내면 설명 요구.
- **일정**: 2026 마감은 1월 28일. 2027년 1월 하순 가정. URP 종료 2026-12-18.
  → **10월 말 측정 freeze, 11월 사례 연구, 12월~1월 집필.** (변경 없음)
- **폴백 벤치**: SCC·비용 두 발견 축이 모두 약하게 나오면 RTA/FSCD(재작성 커뮤니티는
  규모 + 비용 특성화를 높게 평가) 또는 OOPSLA. CAV 조기 리젝트 통보가 3월이라
  재포고 여유는 있다.

---

## 1. 헤드라인 주장 — 축별로, 실측 그대로

rev.1의 "6만 규칙 규모 게이트"는 **합류성 축에선 성립하지 않는다.** 실측 집계
(verification.md tip `eda41eed`, 565심볼 = 슬라이스 가능한 574 중 규칙 0인 9 제외):

| 축 | 측정 | 판정 분포 | **검사 성공한 최대 슬라이스** |
|---|---|---|---|
| termination | 308/565 (54.5%) | YES 307 · TIMEOUT 1 | `Program_ok` **60,723규칙** (20,480s rung — 이 초는 비용이 아니라 **상한**) |
| confluence | **169/565 (29.9%)** | YES 150 · YES\* 6 · TIMEOUT 13 | `$flatten_annotationList` **868규칙** (1,976.0s) |

868이 진짜 천장이라는 것도 이제 실측이 뒷받침한다 — **바로 위 874규칙부터 885규칙까지
6개가 전부 TIMEOUT**이다(`$flatten_parameterList` 874, … `$callableId` 885, 예산
2,055~2,058초). 규모 곡선이 그 구간에서 꺾인다.

**그러므로 논문에서 쓸 수 있는 문장은 이것이다:**

> 실제 산업 언어(P4) 명세의 결정적 함수 정의부에 대해, **종료성은 최대 60,723규칙
> 슬라이스까지, 합류성은 최대 868규칙 슬라이스까지** well-definedness를 기성 검사기로
> 닫았다. 문헌에서 합류성이 실제로 *검사된* 최대 사례는 수십 등식 규모의 교과서·프로토콜
> 예제이고, 조건부 재작성 벤치마크(COPS/CoCo의 CTRS 부문)는 문제당 수~수십 규칙 규모다.

정직하게 써도 주장은 죽지 않는다. **죽는 것은 61k를 두 축에 다 붙였을 때다** — T1과
§1을 대조하는 데 30초 걸리고, 그 사이 나머지 수치의 신뢰도까지 같이 죽는다.

**같이 나가야 하는 두 번째 문장(= 두 번째 발견, §5 참조):**

> 이 규모에서 합류성 검사 비용을 지배하는 것은 **규칙 수가 아니라 정의의 형태**다.
> **규칙 수를 통제해도** 비용이 갈린다 — 둘 다 202규칙인 `$bin_band`는 142.9초에 닫히고
> `$bin_bor`는 1,792.7초 걸린다(12.5배). ~200규칙 구간 전체로는 44.0초(200규칙)부터
> 예산 초과(>4,080초, 201규칙)까지 **90배 이상** 벌어지고, 868규칙 슬라이스가 1,976초에
> 닫히는 반면 285규칙 슬라이스는 4,090초 예산을 넘긴다.

**⚠️ rev.3 주의 — 두 번째 문장의 증거 우선순위**: 첫 번째로 드는 대조는 **동일 202규칙
쌍**이어야 한다. "44초 vs 4,080초"는 규칙 수가 1 차이(200 vs 201)라 리뷰어가
"경계 효과 아니냐"고 물을 수 있지만, 202 vs 202는 그 반론이 불가능하다. rev.2는 이
데이터를 표에 갖고도 안 썼다.

이 두 문장이 §1 첫 페이지에 있어야 2단계 리뷰의 1라운드를 통과한다.

---

## 2. 핵심 novelty — 재프레이밍 (⚠️ rev.2 대비 **전면 개정**)

### 2.1 문헌 확인 결과 (실험 ⑪, 2026-07-27 완료) — rev.2 주장이 반증됐다

rev.2 §2.2는 "ITP 계열은 패턴 매칭 컴파일로 겹침을 없애므로 **CR을 검사하지 않는다**"고
썼다. **1차 문헌 확인 결과 이것은 거짓이다.**

| 시스템 | 겹치는 절을 어떻게 다루는가 | 합류성에 해당하는 검사가 있는가 |
|---|---|---|
| **Isabelle/HOL `function`** | 겹침 허용 | **있다.** pattern completeness 의무 + **pattern compatibility 의무 — 겹치는 패턴 쌍마다 우변이 같을 것**, 쌍당 서브골 하나 |
| **Isabelle/HOL `fun`** | `sequential`로 전처리해 서로소화 | 없음(겹침이 제거됨). `pat_completeness auto` + `lexicographic_order` 자동 |
| **HOL Light** | 겹침 허용 | **있다** — 겹치는 패턴이 같은 값을 내는 것을 증명 |
| **HOL4** | 패턴 컴파일로 서로소 패턴 집합으로 변환 | 없음(구조적 회피) |
| **Coq / Lean** | 의존 패턴 매칭, **first-match** — 절 순서가 의미를 갖는다 | 없음(구조적 회피) |
| **ACL2 `defun`** | 패턴 매칭 자체가 없다 — 심볼당 단일 등식 | 해당 없음(겹침이 발생 불가). 종료성(measure) + guard만 |

즉 **"함수 정의부의 겹치는 절이 결정성을 위협한다"는 문제의식은 이미 정식화돼 있고,
Isabelle/HOL·HOL Light는 그것을 증명 의무로 낸다.** rev.2가 "가장 강한 반례 후보"로
지목한 ACL2는 오히려 반례가 아니다(겹침이 없어 문제가 성립하지 않는다).

### 2.2 그래서 남는 차별점 — 다섯 축, 전부 실측 가능

Isabelle `function`이 내는 compatibility 의무는 **다음 조건 아래의 근원 임계쌍
결합성**이다: 패턴은 **생성자 패턴**, 패턴은 **선형**, 가드는 이미 바인딩된 변수에 대한
**부울 식**, 시그니처는 **단일 sort(HOL 타입)**, 의무는 **정의 하나씩 대화형 전술로 방출**.
우리 대상은 다섯 축 전부에서 그 밖에 있다.

1. **조건이 자유변수를 바인딩한다 (3-CTRS).** 우리 전제는 `$f(x) = y` 꼴로 **좌변에 없는
   `y`를 새로 묶는다.** Isabelle의 가드는 바인딩된 변수에 대한 부울 식이라 이 형태가
   아니다. 그래서 우리 의무는 "두 우변이 같다"는 등식이 아니라 **조건부 임계쌍의
   결합성**이고, unraveling이 필요한 이유가 정확히 이것이다.
   → §4 [C] "정의-bound 변수 조건", 기여 (ii)의 본체.
2. **좌변 패턴이 생성자 패턴이 아니다.** opaque 매처가 헤드에 온다 → **비근원 임계쌍**이
   생긴다. 생성자 기반이면 근원 겹침만 보면 되지만 우리는 아니다.
   → §4 [C] "opaque 매처 강등".
3. **좌변이 좌선형이 아니다.** 같은 변수가 좌변에 두 번 온다.
   → §4 `linearize_lhs`, 그리고 그것이 SC 결정절차의 요구라는 §5 논거.
4. **시그니처가 order-sorted다.** 서브소트 정보가 겹침의 대부분을 제거하고, 지우면
   spurious CP로 YES가 불가능해진다. 이건 실험 ⑤가 실측으로 증명한 유일한 항목이다
   (13규칙 `$join_text`: sorted 검사기 0.7초 YES, 무sort 도구 4개 전부 실패).
5. **결정절차 대 대화형 방출, 그리고 규모.** Isabelle의 compatibility 의무는 HOL 등식
   목표라 임의로 어려울 수 있고 사람이 닫는다. 우리는 565심볼 · 최대 6만 규칙을 대상으로
   **자동 결정절차/반결합 검사기를 돌린다**. 정의 하나씩 사람이 닫는 방식으로는 도달할 수
   없는 규모다.

**§7에 이 형태로 쓴다 (기존 §2.2 문단을 대체):**

> 함수 정의부의 겹치는 절이 결정성을 위협한다는 문제의식은 증명보조기에 이미 있다 —
> Isabelle/HOL의 `function`과 HOL Light은 겹치는 패턴 쌍마다 우변이 일치한다는 증명
> 의무를 낸다. 그러나 그 의무는 **생성자·선형 패턴**, **이미 바인딩된 변수에 대한 부울
> 가드**, **단일 sort** 아래에서 정의되고, **정의 하나씩 대화형으로** 방출된다. 번역된
> 산업 명세의 정의부는 그 네 가정 모두를 위반한다 — 전제가 자유변수를 묶고(3-CTRS),
> 헤드 패턴이 생성자가 아니며, 좌변이 비선형이고, 시그니처가 order-sorted다. 우리는 그
> 설정에서 조건부·order-sorted 합류성 검사를 565심볼 규모로 돌리고, 무엇이 통과하고
> 무엇이 왜 실패하는지를 보고한다.

이 형태의 이점: (a) Isabelle을 **반례가 아니라 우리 문제의 정당화**로 쓴다 — "이 문제가
중요하다는 것은 ITP 공동체가 이미 인정한 바다"가 CFP 루브릭의 "해결하는 문제의 중요성"에
정확히 답한다. (b) 다섯 축이 전부 §4 카탈로그의 행과 1:1이라 novelty와 본문이 붙는다.
(c) 규모·비용 축(기여 (iii))이 novelty 문장 안에 자연스럽게 들어간다.

**⚠️ 남은 확인 (2차 문헌, 9월)**: Isabelle `function`의 compatibility 의무가 **조건부
등식(가드)일 때 가드를 가정으로 포함하는지**를 1차 문헌으로 확정해야 한다. 공식 문서는
"짐작할 수 있을 것"이라고만 하고 명시하지 않는다. 포함한다면 축 1의 논거는 "가드가
자유변수를 못 묶는다"에만 의존하게 되므로 문장을 그 한 점에 집중시켜야 한다.
지금은 **부분 확인**으로 기록한다.

### 2.3 유지되는 대비부

- **K 프레임워크**: confluence를 검사하지 않고 구조적으로 회피 — 의미론 전체를 비결정
  규칙에 넣고, CR/coherence는 builtin 데이터 층만 by-construction 가정, equation/rule
  분류는 사람이 손으로 준다(Arusoaie et al. WRLA'14; C semantics POPL'12, 1,163규칙).
- **P4K** (Kheradmand–Roșu, arXiv:1804.01468): **같은 언어(P4)** 의미론 ~400규칙,
  자동 게이트 없음, 모호성은 손으로 발견 → §7 정면 대비.
- **SpecTec 원논문** (Youn et al. OOPSLA'24): 메타 타입체크 + per-theorem Coq
  soundness 생성 → per-spec well-definedness 게이트가 없다. 핵심 인용·차별화.
  (**이중맹검**: "우리 DSL"이라 부르지 말고 3인칭으로. 부록 C.)
- **합류성 검사기 실증의 스케일 천장** — 문헌 최대 사례가 수십 등식이고, 조건부 재작성
  벤치마크(COPS)의 CTRS 부문은 **문제 총수가 161개**(2023년 기준)인 소규모 문제 모음이다.
  PL 의미론·production 규모 적용 사례 0.
  (⚠️ **문제당 규칙 수는 아직 미확인** — 부록 A.3-7. 확정 전까지 "문제당 N규칙" 형태의
  문장을 쓰지 않는다.)

### 2.4 프레이밍 금지 사항

1. **검사기 자체를 신규라 주장 금지** — 도구는 이미 존재·자동화돼 있다.
2. **"합류성을 아무도 검사하지 않는다" 금지 (rev.3 신규)** — 반증됐다. 쓸 수 있는 것은
   "우리 설정의 네 가정 위반 아래에서, 그리고 이 규모에서는 없다"뿐이다.
3. **proof-score 전통**(SC + ground confluence 대화 검사 도구가 중심)은 프로토콜/대수
   데이터형이 대상이라, 주장을 "production-scale PL semantics"로 스코프하면 반례가
   아니다 → §7 각주 한 줄로 선점 처리(본문 문단을 쓰지 않는다).
4. **"검사기 제약이 우리 표면 설계를 강제했다"** 라즘(기여 (ii))을 붙이면 "패스들이
   ad hoc 해킹"이라는 공격도 함께 막힌다.

---

## 3. 주장 스코프 (§2에서 못박는다)

**대상**: SpecTec 명세, 정확히는 P4 방언을 실행하는 일라보레이티드 IL → 그 **결정적 함수
정의부**의 well-definedness:

1. 종료성 (SN)
2. 합류성 (CR)
3. **pattern completeness** — sufficient completeness의 무조건 근사
4. **sort-decreasingness** — order-sorted에서 유일 정규화 라거의 필수. 합류성 검사기의
   YES 판정에 이미 포함된 조건이므로 등식에 명시한다.

**대상 밖**: 비결정적 축약 관계(번역 결과의 상태 전이 규칙이 0개 — 전부 조건부 등식),
LTL/시간적 성질(§8 결정 기록), 정적 의미 보존 증명(§6 공개; differential testing이
경험적 대체 증거).

**⚠️ 스코프 제한 (실험 ⑨ 대상)**: `== false`(does-not-hold) 근사를 포함하는 슬라이스는
**합류성 주장에서 분리해 보고**한다. 그 근사는 stuck 의미를 결정화하므로 합류성에
**불건전한 방향**이고, YES가 원 시스템으로 전이한다고 말할 수 없다. 분리 비율은 실험 ⑨가
측정한다. 그 수치 없이 §5를 쓸 수 없다.

**rev.3 추가 근거 — 이 근사는 SCC 실측에서도 지배 요인으로 확인됐다.** `subty_` 계열
SCC 반례 405건(`dom:Val-wide`)의 원인이 정확히 **보완(complement) 절이 무관한 소스 타입까지
넘나드는 것**이었다(todo.md 2026-07-14). 즉 `== false` 근사의 부채는 합류성 축과 SC 축에
**동시에** 나타난다 — §4의 해당 행을 두 축 공통 행으로 올리고 §5에서 한 번에 정량화한다.

---

## 4. 두 검사 축의 분리 (압축표 — 논문에는 이 표를 그대로 옮기지 않는다)

> **경고**: 아래는 작업 문서용 전체 표다. 논문 §3에는 **4행 압축표 + 반 페이지 산문**만
> 들어간다. 이 표를 이식하면 §3에서 1페이지를 쓰고 §4·§5가 죽는다.

| | **종료성** | **합류성** |
|---|---|---|
| 검사 성질 | strong normalization | Church-Rosser + sort-decreasingness |
| YES의 의미 | 모든 항이 정규형에 도달 | 유일 정규형 (결정성) |
| 입력 표면 | **sort 버림** (무sort TRS) | **sort 유지** (order-sorted CTRS) |
| 적용 변환 | unraveling (CTRS→무조건 TRS, keep-constructor) | 정규화 패스 + opt-in unravel |
| 건전성 방향 | **over-approximation** | **혼합** — 대부분 대칭, unravel만 **reflect-only** |
| YES 전이 | TRS SN ⟹ CTRS SN (**하강** 없음) | (WLL 하) U(R) CR ⟹ R CR (**상승**) |
| NO/MAYBE | 근사 아티팩트 — 비종료 witness 아님 | reflect-only의 NO/MAYBE는 **무의미**, YES만 채택 |
| sort 처리 | **버림이 안전** (같은 over-approx 방향) | **유지** — erasure는 spurious CP로 YES를 불공정하게 어렵게 함 |
| 필수 조건 | unravel의 바인더 대응 · 조건 binding 순서 · 커버리지. **WLL 무관** | **WLL**(슬라이스별 검증), sort-decreasingness, owise 반사 건전성 |
| 불건전 방향 | 없음 | **`== false` 근사** (§3 스코프 제한, 실험 ⑨) |

**한 줄 요약 — 종료성**: 무sort TRS로 over-approximate해 SN을 증명한다. YES는 하강
전이하고, NO는 근사 아티팩트라 witness가 아니다. 변환은 신규가 아니며 기여는 **진단 +
규모**다.

**한 줄 요약 — 합류성**: order-sorted CTRS를 그대로 태운다. **sort 유지가 핵심** —
erasure는 spurious CP로 YES를 불공정하게 어렵게 만든다(실험 ⑤: 13규칙 `$join_text`를
sorted 검사기는 0.7초 YES인데 무sort 도구 4개가 전부 실패). 정규화 패스는 대부분
대칭이지만 opt-in unravel은 reflect-only라 **WLL 하에서 YES만 상승**시키고 NO/MAYBE는
버린다 → 종료성의 over-approximation과 **방향이 반대**다.

**없으면 무너지는 것**: (1) "unravel 먹이고 무sort 도구로"를 합류성에 쓰면 안 된다 —
reflect-only라 NO/MAYBE가 무의미(실측: confluent `$join_text`가 unravel 후 무sort
도구에서 NO). (2) sort 유지 라리를 종료성에 요구하면 안 된다 — 버려도 안전. (3) "YES만
신뢰"는 둘 다 같지만 **이유가 다르다**(하강 없음 vs WLL 상승).

---

## 5. 기여 (§1 불릿 — 재편)

**핵심 변경**: (a) rev.1의 (ii)(종료성 진단)를 불릿에서 내려 §4 카탈로그로 흡수한다 —
"주장 철회한 표 남은 것"을 §1 불릿으로 두면 자기 약점을 목차에 새기는 셈이다.
(b) **비용 특성화를 독립 기여로 승격**한다 — SCC가 0이 나와도 논문이 성립하려면 발견
축이 둘이어야 한다.

**§1에서 이름으로 호출할 정리 (CFP 루브릭 대응)**: 임계쌍 보조정리(order-sorted 조건부
확장), Marchiori–Ohlebusch unraveling의 종료성 반사, Hendrix–Meseguer–Ohsaki의 sufficient
completeness 결정성(left-linear + unconditional). "우리는 이 정리들을 6만 규칙 규모의
실제 언어 명세에 적용한다"가 트랙이 요구하는 문장 형태다.

1. **(i) 번역 + 장애물 카탈로그** — 추론 규칙 기반 명세 DSL의 결정적 조각 → order-sorted
   조건부 등식. 프레이밍은 "**장애물 카탈로그 + 레시피, P4 명세로 인스턴스화**".
   분명한 장애물(정의-bound 변수 → spurious CP, opaque matcher 강등, fallthrough
   negation, iteration binder, sort erasure)이 특정 DSL의 특이점이 아니라 **추론규칙
   표기 자체의 산물**임을 §4 표의 행별로 라즘.
   → "general"은 두 번째 명세 인스턴스(⑫) 확보 전에는 쓰지 않는다.
   → **rev.3**: 이 다섯 장애물이 §2.2의 다섯 차별 축과 정확히 대응한다는 것을 §4 도입부에
     한 문장으로 박는다. novelty 문장과 본문이 붙는 지점이 여기다.

2. **(ii) 검사기가 강제하는 부작용 없는 표면 정규화** — 매처 강등 재철자, 멤버십 강등 제거,
   정의 바인더 인라인, 조건 방향·순서 규약, fallthrough 절 타입-disjoint 강등 반사,
   upgrade-only 정규화. 각 패스의 근사 방향(equivalence / reflect-only / 미증명)은 §4에
   명시.
   **핵심 라거**: 이 패스들은 취향이 아니라 **검사기가 요구하는 형태**다 — 사용 검사기는
   built-in 이론 금지 · `owise` 금지 · operational termination 선증명을 요구하고, SC
   결정절차는 left-linear + unconditional에서만 결정 가능하다. 그래서 `owise` 드롭과
   별도 종료성 경로는 임의적 선택이 아니다. (이 라거가 "ad hoc 해킹" 공격의 방벽이다.)
   여기에 종료성 축의 진단(기성 변환기의 인코딩이 매칭 조건을 3형 extra-var CTRS로
   만들어 MAYBE를 부르고, 표준 unraveling으로 되돌리면 닫힌다)을 **카탈로그 한 행**으로
   붙인다.

3. **(iii) 규모 평가 + 비용 특성화** — *승격*
   - 565심볼 대상, 판정·시간·ablation·baseline. 규모 수치는 **축별로**(§1 표).
   - **비용은 규칙 수로 예측되지 않는다**: 동일 202규칙에서 142.9초 vs 1,792.7초(12.5배);
     ~200규칙 구간 전체로 44.0초 ~ >4,080초(90배 이상); 868규칙 YES(1,976초) vs 285규칙
     예산 초과(4,090초); 그리고 **868 바로 위 874~885규칙 6개가 전부 예산 소진**.
   - **예산 초과 13건의 구성**(rev.3 정정): 비트 조작 7건(`$write_bits_from_value` 103,
     `$bin_shl` 201, `$bin_shr` 215, `$bitacc_range_op` 283, `$bitacc_offset_op` 285,
     `$bitacc_range_replace_op` 391, `$bitacc_offset_replace_op` 394) + **리스트 평탄화·
     식별자 계열 6건**(874~885규칙). rev.2의 "TIMEOUT 전부 비트 조작"은 더 이상 참이 아니다.
   - **팔 수 있는 결론**: 어떤 명세 idiom이 게이트를 통과 못 시키는가 — 스펙 저자에게
     주는 조언. 이게 application 논문의 값이다.
   - **문헌 앵커(negative result)**: 큰 실제 의미론에 *존재하는* 합류성이 검사된 적이
     없다 — C semantics 1,163규칙에 confluence 언급조차 없고, 조건부 재작성 벤치마크
     (COPS/CoCo CTRS 부문)는 소규모 문제 모음이다(2023년 기준 문제 161개).
     § T1을 "큰 건 안 쟀고, 잰 건 교과서 크기"로 프레이밍.

4. **(iv) 게이트로서의 배치 가능성** — *신설*
   전체 스윕은 감사(audit)이고 게이트가 아니다(단일 심볼 최대 3,198초, 전체는 며칠).
   심볼별 dependency-closure 슬라이싱은 도구가 못 버텨서 만든 우회가 아니라 **변경된
   심볼과 그 역의존만 재검사하는 증분 CI 게이트를 가능하게 하는 장치**다. 실험 ⑩이
   커밋당 증분 비용을 측정한다. 이 불릿이 "그래서 누가 이걸 돌리나"에 답한다.

5. **(v) 발견** — SCC 실측 → triage → spec 결함 후보 (실험 ③).
   **rev.3: 결과의 성격이 바뀌었다.** `subty_`/`match_` 951심볼 전수 스윕에서
   **`dom:narrow`(진짜 누락) 0건 · 503심볼 COMPLETE**가 나왔다. 즉 이 불릿은 "결함을
   찾았다"가 아니라 **"503심볼의 전체성을 기계로 증명했고, 남은 반례 448건은 전부 근사
   부채로 귀속된다"**로 쓴다. 후자가 오히려 §5의 비용/근사 정량화와 직결된다.
   나머지 가족(함수 435 / relation·프리루드 285 / `holds_` 51 / `approx` 756)이 남았고,
   여기서도 0이면 (iii)의 비용 발견이 발견 축을 단독으로 지탱한다.

---

## 6. 섹션 계획 (10p + 부록)

> **페이지 예산은 사전에 둔다.** rev.1은 합이 정확히 10.0p였고 표 5개가 2.25p에 들어가
> 있었다 — 불가능하다. 표 2개만 본문, 나머지는 부록.

| § | 내용 | p |
|---|---|---|
| §1 | Introduction | 1.5 |
| §2 | Background + 주장 범위 | 1.0 |
| §3 | Translation | 1.75 |
| §4 | Making it checkable (심장) | 2.0 |
| §5 | Evaluation | 2.0 |
| §6 | Threats to validity | 0.5 |
| §7 | Related work | 0.75 |
| §8 | Conclusion | 0.25 |
| | **합** | **9.75** (여유 0.25) |

### §1 Introduction (1.5p) — 2단계 리뷰 대응 구조

**순서를 뒤집는다.** (1) 문제 한 단락: 기계화 명세의 함수 정의부는 겹치는 절과 바인딩
전제를 갖는데, 그 well-definedness를 아무도 확인하지 않는다. (2) **결과 두 문장을
여기서** — §1의 두 헤드라인 문장(규모, 비용 비예측성). (3) 왜 재작성 시스템인가.
(4) 적용한 정리 3개 명명. (5) 기여 불릿.

**왜 재작성 시스템인가 (한 단락)**: 검증하려는 성질이 정확히 재작성 이론의 고전
성질이라 성숙한 기성 검사기와 대회(termCOMP/CoCo)가 있고, IL의 결정적 조각(전제 달린
함수 절)이 조건부 등식과 구조적으로 1:1이기 때문. **그리고 §2.2의 라거**: 이 문제를 아는
ITP 함수 패키지들도 자기 가정(생성자·선형 패턴, 부울 가드, 단일 sort, 대화형 방출) 밖으로
나가지 못한다. 대안 비교: 증명보조기(명세마다 임의성, 확장성), Twelf `%total`(LF
재인코딩), 실행 중심 프레임워크(well-definedness 게이트 없음), 테스트(보장 없음).

### §2 Background + 주장 범위 (1.0p)
예시 규칙 하나. well-definedness 등식 4항 명시 + 계열 인용. **결정적 정의부만**이라는
스코프 선언 + **`== false` 슬라이스 분리 선언**(§3 스코프 제한).
**무조건 근사(pattern completeness)의 근거**: 사용하는 SC 결정절차는 left-linear +
unconditional에서만 결정 가능 → `drop_conds`/`linearize_lhs`는 취향이 아니라 결정절차
클래스가 강제하는 형태다(조건부를 다루는 narrowing+ITP 방식은 결정절차가 아니다).
근사 방향 명시: 조건을 떨어내면 절이 더 넓게 적용되므로 **커버리지가 과대평가**된다 →
**보고된 gap은 실재하고, YES는 pattern completeness까지만**이다.

### §3 Translation (1.75p)
대상 fragment를 **요구 조건 R1~R3**으로 정의 → 이 방언은 by construction 만족, 타 방언은
무엇이 필요한지 한 단락:
- **R1 — 일라보레이티드 IL**: variant/struct/list/option/iteration이 명시적.
- **R2 — 관계의 입력-모드 규율**: 모든 관계가 입력-모드 = 결정적·함수적 → 조건부 등식으로
  번역 가능. 이 방언은 일라보레이터가 강제(모든 포트 파싱, 무포트 관계는 경고 후 전-입력
  기본). upstream IL엔 모드-태그 관계 타입 자체가 없다.
- **R3 — 생성자 판별 규율**: SC/pattern completeness의 전제.

타 방언·타 DSL은 모드 주석 또는 모드 추론으로 R2를 확보하고, 비결정 서술 관계는 fragment
밖. 인코딩 규칙(before/after 한 쌍), iteration 컴파일, prelude, scalar theory 2종.
**성질 전달은 §4 압축표를 참조**하고 여기서 재서술하지 않는다.

**검사기 독립성 한 줄 (탈-도구화의 라거)**: 파이프라인은 구조적 CTRS를 산출하고, 특정
검사기에 맞춘 방출은 단일 백엔드 모듈로 격리돼 있다 → 코드 구조가 그렇다(`rewrite`
라이브러리의 백엔드 서브디렉터리). 방법은 검사기 특이적이지 않고, 다만 **order-sorted +
조건부를 동시에 받는 검사기가 사실상 하나뿐**이라는 도구 공백이 선택을 결정한다(실험 ⑤).

### §4 Making it checkable (2.0p) — 심장
장애물 카탈로그. 각 행: **[축]** 장애물 → **표기의 어떤 특징이 원인인가** → 해법 →
근사 방향(equivalence / reflect-only / 미증명) → §5 ablation 행 1:1 대응.

**도입부 두 문장 (rev.3 신규)**: (a) 이 카탈로그의 다섯 행이 §2.2의 다섯 차별 축과
1:1이다 — 즉 이 표가 novelty 주장의 증거다. (b) 관찰 하나를 앞에 세운다:
**checkability 장애물이 거의 전부 합류성 축에 몰려 있다** — 종료성은 표준 unravel만
제대로 하면 깨끗하고([T] 한 행), 정규화 노동의 대부분이 합류성 검사기를 통과시키는 데 든다.
이 편중 자체가 두 축의 성격 차이를 보여준다.

- **[T]** unraveling의 subterm 손실 (조건을 대칭 동등성 검사로 인코딩하는 기존 방식과
  대비 — 매칭으로 묶인 변수가 흘려 평가가 안 보인다). over-approximation, YES 하강.
  → 종료성 진단(기여 (ii))이 이 행에 들어간다.
- **[C]** 단일 sort의 spurious CP (order-sorted 서명 **유지**; erasure는 YES를 불공정하게
  어렵게 함 → 실험 ⑤가 유일 증거). ↔ **§2.2 축 4**
- **[C]** opaque 매처 강등 → head 패턴 destructure로 재철자. 대칭. ↔ **§2.2 축 2**
- **[C]** fallthrough 절의 negation 벽 → 타입-disjoint 강등 반사. 대칭(반사 건전성 §6).
- **[C]** 좌변 비선형성 → `linearize_lhs`; SC 결정절차가 요구하는 형태. ↔ **§2.2 축 3**
- **[C]** 정의-bound 변수 조건 (determinacy critical pair; 단일변수 바인더 인라인 = 대칭 +
  opt-in unravel = **reflect-only**, WLL 하 upgrade-only). ↔ **§2.2 축 1** — 두 축이
  unravel이 가리는 지점.
- **[C+SC]** `== false` 근사 → **여기서 불건전 방향을 정직하게 선언**하고 실험 ⑨의 분리
  비율을 적는다. §6으로 미루지 않는다(본문에서 먼저 말하는 편이 방어적으로 유리).
  **rev.3**: 이 행은 합류성 축 단독이 아니라 **SC 축과 공통**이다 — SCC 반례 405건의
  원인이 같은 보완 절이다(§3 스코프 제한 참조).
- **[T+C]** 대형 슬라이스 → 심볼별 dependency-closure 슬라이싱. 두 축 공통. **여기서
  기여 (iv)로 연결** — 이 슬라이싱이 증분 게이트를 가능하게 한다.

### §5 Evaluation (2.0p)
- **보고 원칙**: attempted / YES / YES\*(정규화 승격, upgrade-only) / MAYBE / TIMEOUT을
  전부 구분 보고. YES\*를 각주 없이 YES에 합산하지 않는다. **TIMEOUT은 "미측정"이 아니라
  측정 결과다.** 미측정은 `-`로 별도.
- **수치는 freeze 커밋 단일 트리에서 재측정한 것만** 싣는다(실험 ⑧).
- **커버리지를 숨기지 않는다**: 합류성 169/565가 표 캡션 첫 줄에 있어야 한다. 미측정
  396개가 전부 대형 슬라이스(**784규칙 이상**)라는 사실도 같이.
  (rev.2의 "≥874규칙"은 오류 — 실제 하한은 784다.)

**본문 표·그림 (2개만)**:
- **F1 — 비용 산점도** (x = 슬라이스 규칙 수, y = 벽시계, 점 모양 = 판정).
  기대 효과: 비단조성이 한눈에 보인다. **동일 규칙 수 쌍(202/202)을 주석으로 찍는다** —
  이게 이 그림의 결정타다. 868규칙 YES 1,976초와 285규칙 >4,090초, 그리고 874~885
  구간의 예산 소진 벽이 같이 보인다. **이 그림이 논문의 얼굴이다.** (①⑧)
- **T1 — 축별 규모·판정 요약 + scale-ceiling 대비 4행**: (C 1,163규칙/미검사, JavaFAN
  Java 수백 등식/가정, 조건부 벤치마크 CTRS 부문/소규모 문제 모음, 검사기 데모 수십
  등식/검사) vs (우리: 종료성 60,723 / 합류성 868). (기여 (iii) 근거)
  **⚠️ 이 표의 대비 행 중 JavaFAN·벤치마크 규모는 1차 확인 전이다 — 부록 A.3-7.**

**부록으로 내리는 표**:
- **A1 — 심볼별 전체 판정 표** (verification.md 그대로).
- **A2 — 번역·정규화 ablation**: 행 = §4 장애물/패스(1:1), 열 = affected symbols·판정
  변화·Δ시간. **층화 표본 기준 명시.** (④)
- **A3 — 외부 도구 baseline** (두 축 분리). (①⑤)
- **A4 — SCC triage** + **A5 — 증분 게이트 비용**(⑩) + **A6 — `== false` 분리 집계**(⑨).

본문에는 A2~A6 각각을 **한 문장 + 부록 포인터**로 요약한다.

### §6 Threats to validity (0.5p)
- **`== false` 근사** → §4에서 이미 선언했으므로 여기선 분리 비율과 그 밖 슬라이스의
  YES가 무엇을 의미하는지만. (실험 ⑨)
- **WLL의 전체 표면에서 global 불성립**(위반 273 → 바인딩된 변수의 조건 패턴 재사용).
  승격의 건전성 전제는 **슬라이스별** WLL 검증으로 확보(승격 슬라이스 6 + `$join_text` +
  예산 초과 7 전부 clean 실측). **승격 절차에 슬라이스별 WLL 검사 내장은 미완 → 공개.**
  → freeze 전에 내장하지 못하면 **YES\* 6건을 헤드라인에서 빼고 부록에만 둔다.**
- fallthrough 절: 반사되지 못한 remainder가 검사기 자체의 default-절 처리에 의존 → 그
  처리의 건전성 미확인.
- gensym: 축약 순서에 따른 fresh 이름 분기 = 항 차원의 비합류 원천.
- 미증명 패스: 정의 바인더 fold의 spurious-CP 주장(단락 라즘 + differential 증거로 대체),
  co-iteration projection fusion의 근사 방향.
- 무조건 근사 SC: YES는 pattern completeness까지만.
- 의미 보존 정적 증명 부재; differential testing이 경험적 증거.
- **커버리지**: 합류성 미측정 396개가 전부 대형 슬라이스(≥784규칙)이므로, 대형
  슬라이스의 판정 분포가 측정된 169와 다를 수 있다(selection bias). 명시.
  **관측된 방향까지 적는다** — 874~885규칙 6개가 전부 예산을 소진했으므로, 편향은
  "낙관적"일 가능성이 높다.

### §7 Related work (0.75p) — 배치 순서는 §2의 논증 구도를 따른다
1. **함수 정의 admission 게이트 (가장 가까운 이웃)** — Isabelle/HOL `function`(Krauss)와
   HOL Light은 **겹치는 패턴 쌍의 우변 일치를 증명 의무로 낸다**; HOL4는 패턴 컴파일로
   겹침을 없애고, Coq/Lean은 first-match로 순서를 의미화하며, ACL2 `defun`은 패턴 매칭
   자체가 없어 겹침이 발생하지 않는다(종료성 measure + guard만).
   **차별점은 §2.2의 다섯 축**: 생성자·선형 패턴, 부울 가드, 단일 sort, 대화형 방출.
   Twelf `%mode`/`%terminates`/`%covers`/`%total`도 여기 → 추론규칙 명세의 메타 성질
   자동 검사라는 점에서 개념적으로 가깝고, 차별점은 LF 재인코딩 필요 vs 기성 DSL 그대로.
   **⚠️ rev.3: 이 문단이 논문의 신뢰도를 결정한다.** "그들은 검사하지 않는다"로 쓰면
   Isabelle을 아는 리뷰어 한 명에게 통째로 넘어간다.
2. **실행 중심 의미론 프레임워크 + P4 사례** — 같은 분류 DSL인데 well-definedness 게이트가
   없다: CR/coherence를 builtin 층만 가정하고 equation/rule 분류는 수동;
   **같은 언어(P4) ~400규칙 의미론이 자동 게이트 없이 모호성을 손으로 발견** → 정면 대비.
3. **SpecTec OOPSLA'24** — 메타 타입체크 + per-theorem Coq soundness, per-spec
   well-definedness 게이트 없음. Ott/Lem/Sail, PLT Redex도 같은 분류(번역기·타입/건전성·
   테스트, 게이트 없음). **3인칭 서술.**
4. **unraveling·종료성 계보**: Marchiori (ALP 1996), Ohlebusch (LPAR 1999), Gmeiner–Nishida
   (WPTE 2014) taxonomy, Nishida–Sakai–Sakabe (LMCS 2012), Lucas–Meseguer operational
   termination + 2D DP.
   ⚠️ **"structure-preserving" 표현 금지** — 다른 분류의 고유 명칭이라 오독된다.
   "keep-constructor packaging"으로 부른다.
5. **재사용한 검사 이론·상태계**: rewriting logic semantics, 조건부·order-sorted 합류성
   검사, CoCo/termCOMP, sufficient completeness 결정절차 (Hendrix–Meseguer–Ohsaki).
   proof-score 전통(SC + ground confluence 대화 검사)은 **각주 한 줄**로 스코프 선긋기.

### §8 Conclusion (0.25p)
LTL / 비결정 축약 관계는 future work 한 문장. 대형 슬라이스 합류성 커버리지도 한 문장.

---

## 7. 실험 계획

### 7.1 측정 프로토콜 — "전수"를 버리고 고정 예산 + 층화로 대체

rev.1의 ⑥ "잔여 402를 8월에 전수", ⑧ "10월 말 3검사 전수 재측정"은 성립하지 않는다.
285규칙 슬라이스가 4,090초 예산을 넘기는데 잔여 396은 **전부 784규칙 이상**이고, 실측된
874~885규칙 6개가 전부 예산을 소진했다. 전수는 불가능하다.

**대체 프로토콜 (freeze 전에 확정해 문서화)**:

1. **고정 예산**: 심볼당 합류성 예산 T_c, 종료성 예산 T_t를 하나로 고정한다(사다리 금지 —
   사다리는 "답하는 최소 예산"의 보장이 대형 구간에서 이미 깨져 있고, 논문의 초가 비용이
   아니라 rung이 된다). 넘으면 **TIMEOUT을 판정으로 기록**하고 넘어간다.
   **⚠️ 현재 표는 예산이 섞여 있다** — 2,040 / 2,055~2,058 / 4,080~4,090 / 20,480초.
   그래서 "TIMEOUT 13"을 서로 비교할 수 없다(103규칙 `$write_bits_from_value`는 2,040초만
   받았고 201규칙 `$bin_shl`은 4,080초를 받았다). freeze 재측정의 첫 번째 이유가 이것이다.
2. **층화 표본**: 규칙 수 밴드(≤100, 100–300, 300–1k, 1k–10k, >10k)별로 목표 n을 정하고,
   각 밴드에서 무작위 추출. **전수는 ≤1k 밴드까지만.** §5는 "밴드별 커버리지 + 밴드별
   판정 분포"로 보고한다. 이게 silent cap을 피하면서 실행 가능한 유일한 형태다.
3. **보고**: attempted / YES / YES\* / MAYBE / TIMEOUT / 미측정을 밴드별로.

### 7.2 실험 목록

| # | 실험 | 상태·값 | 일정 |
|---|---|---|---|
| ① | ✅ **기성 종료성 검사기의 CTRS 모드 직접 비교** | 완료 2026-07-27. ≤500 153심볼 A/B 판정 153/153 YES 양쪽·불일치 0, 시간 동등 → 내부 unravel이 우리와 동등, **신규 변환 주장 철회**. 인프라 `--ctrs-mode`/`--emit-ctrs`로 재현 가능 | 완료 |
| ② | ✅ **unraveling 문헌 분류 확정** | 완료 2026-07-27. 확정: 표준 Marchiori–Ohlebusch unraveling의 표기 변종(termination 대칭), 다른 분류 아님. "structure-preserving" 표현 금지 | 완료 |
| ⑪ | ✅ **ITP 함수 admission 문헌 확인** | **완료 2026-07-27 (1차).** 결과가 rev.2 주장을 **반증**: Isabelle `function`은 pattern completeness + **pattern compatibility**(겹치는 쌍마다 우변 일치) 의무를 내고 조건부 등식도 지원한다; HOL Light 동일; HOL4는 패턴 컴파일로 서로소화; Coq/Lean은 first-match; ACL2는 패턴 매칭 없음. → §2.2를 다섯 축으로 재작성 완료. **남은 것**: compatibility 의무가 가드를 가정으로 포함하는지 1차 문헌 확정(9월, §2.2 말미) | 1차 완료 |
| ⑨ | 🔴 **`== false` 근사 슬라이스 정량화** — **최우선** | **A2·A6 대상. 이것이 없으면 §5를 쓸 수 없다.** 그 근사를 포함하는 슬라이스를 파이프라인에서 플래그하고, 측정된 169 중 몇 개인지 · 그중 YES가 몇 개인지 집계. 결과에 따라 §1 주장 문장이 바뀐다(예: "169 중 근사-free 슬라이스 N개에서 CR YES"). **rev.3 추가**: SCC `dom:Val-wide` 405건과 같은 원인이므로 **한 번의 계측으로 두 축을 정량화**한다 | **8월 초, 다른 것보다 먼저** |
| ⑥ | **합류성 실측 → 층화 표본으로 전환** | §5 판정 분포의 본체. 7.1 프로토콜 적용. **"잔여 396 전수"는 폐기.** 현재 스윕은 심볼당 프로세스로 진행 중이며 표는 계속 움직인다 — **freeze 전까지의 수치는 전부 잠정** | 8~9월 |
| ③ | **SCC 실측 + triage** | **rev.3: 1/3 완료.** `subty_`/`match_` 951심볼 전수 스윕 완료(2026-07-14) — 951/951 `exact` fidelity, **503 COMPLETE · 448 COUNTEREXAMPLE · `dom:narrow` 0건**. triage 3범주(`dom:Val-wide` 405 / `dom:elem-erased` 43 / `dom:narrow` 0)가 이미 확립돼 있고, 405건의 원인은 보완 절 과확장(=실험 ⑨와 동일 원인)이다. **남은 것**: 함수 435 / relation·프리루드 285 / `holds_` 51 / `approx` 756 스윕 — 슬라이스는 덤프돼 있어 스크립트만 돌리면 된다. 진짜 결함 후보는 `dom:narrow`에서만 나온다 | 8~9월 |
| ④ | **ablation 테이블 — 층화 표본** | CLI `--disable-pass=NAME`. **leave-one-out × 층화 표본**(전수 금지 — 7패스 × 169심볼 × 평균 시간이면 수백~수천 CPU시간. 예산을 먼저 계산해 문서화). 기록: 심볼·config·verdict·시간·CP 수·규칙 수·**emit 표면 해시**(해시 동일 = no-op → "affected symbols 중 verdict 변화"로 집계). 대조군으로 동등성-검사식 unravel 모드를 동일 도구·동일 예산으로. **⚠️ 해석 규약 명시**: 등가 패스를 껐을 때 판정이 바뀌는 것은 "패스가 의미적으로 필수"가 아니라 **"검사기가 표면 형태에 민감"**의 증거이고, 그게 기여 (ii)의 라거다 → 이 문장을 §5에 박지 않으면 리뷰어가 ablation을 오독한다 | 8월 말~9월 |
| ⑤ | **합류성 baseline (무sort 도구군)** | **파일럿 완료(4심볼, 2026-07-27)**. 두 발견: (1) **sort-erasure penalty 실증** — 13규칙 `$join_text`를 sorted 검사기는 0.7s YES인데 무sort 도구 4개 전부 실패 → §4 "단일 sort" 행의 유일 근거 + §2.2 축 4의 실증. (2) **무sort 도구 하나가 `$write_value_from_bits`를 YES로 닫음**(우리는 그 계열이 YES\*) → 무sort YES는 sorted로 하강 전이하므로 유효 증명, **잔여 예산 초과 7건의 백업 후보**. baseline은 "우리 지배"가 아니라 **혼합**(정직히 보고). 단점: COPS export 배관, TIMEOUT 전수 미실행, ≤500 표본. export golden 필요. **서술 방향**: 특정 엔진 우위가 아니라 **"order-sorted + 조건부를 동시에 받는 검사기가 사실상 하나뿐인 도구 공백"** | 9~10월 |
| ⑩ | **증분 게이트 비용** | 기여 (iv)의 유일한 증거. 최근 N개 실제 커밋에 대해 (변경 심볼 + 역의존 폐포)만 재검사한 비용을 측정. "커밋당 중앙 X초, 90th percentile Y초"가 나오면 게이트가 배치 가능하다는 주장이 성립. **싼 실험이고 응용 논문의 값을 가장 많이 올린다** | 9월 |
| ⑧ | **커밋 freeze → 고정 예산·층화 프로토콜 1회 실행** | 현재 어느 커밋도 세 검사를 한 트리에서 가진 적 없다. 조건-방향 규약(`orient_conds`) 재측정 부채와 **예산 혼재**(2,040/2,058/4,080/20,480초)가 합류. **"전수"가 아니라 7.1 프로토콜의 1회 실행.** §5는 이 수치만 사용 | **10월 말** |
| ⑫ | **두 번째 명세 스파이크 — 사례 연구** | (i) 프레이밍의 최소 일반성 증거. 1단계(사전 조사): upstream IL ↔ 이 방언 IL 델타 + 대상 관계의 모드화(R2) 가능성. 이어 **11월 초 1주 timebox**. 실패 시 §4 표의 원인 열 + R1~R3 조건화로 폴백 | 11월 |
| ⑬ | **artifact / double-blind 준비** | 부록 C 체크리스트 전체. **산문 검사가 파일 검사보다 중요하다** | 11월 말 |

**우선순위 근거**: ⑨(주장 유효성·두 축 동시) > ⑩(기여 (iv)) > ⑥④⑤(본체) > ③(발견,
잔여 가족) > ⑧(freeze) > ⑫⑬. ⑪은 완료돼 우선순위에서 빠졌다.

---

## 8. 결정 기록

- **LTL 모델 검사: 이번 논문 제외** (2026-07-26). future work 한 문장. 사유: (a) per-spec
  메타 성질 축이 per-program 성질로 흐림, (b) 상태 전이 규칙 0개·결정적 조각 스코프와 정면
  충돌(coherence 축 부활 필요 → 그 축의 판정이 합류성을 그대로 따라가 무정보로 판명),
  (c) 지면. 비결정 축약 관계 + coherence + LTL은 다음 논문 한 편.
- **SCC 배치**: §2 등식의 구성 요소("pattern completeness") + §5의 발견 축. 별도 기여
  불릿 승격은 잔여 가족 스윕 결과가 나온 뒤 결정.
- **두 번째 명세 > 내부 보조정리** (11월 우선순위): 응용 트랙엔 응용 폭 > 내부 보조정리.
  보조정리는 §6 공개 + 단락 라즘 + differential 증거로 충분. 단 스파이크 실패 시 폴백.
- **§3 축의 judgment 계산 형식화 안 함**: 지면 대비 얻는 게 서사적 일반성뿐. 구체 문법 +
  표기-일반 특징 명시 문장으로 절충.
- **보고 원칙**: YES\*는 별도 등급(신뢰 등급이 다르다), TIMEOUT은 측정 결과, 미측정은 `-`.
- **(rev.2) 규모 주장을 축별로 분리**. 61k는 종료성 단일 심볼이고 합류성 천장은 868규칙.
  두 축에 61k를 붙이는 문장은 금지.
- **(rev.2) 기여 (ii)의 종료성 진단을 §1 불릿에서 §4 카탈로그 행으로 강등.** 대신 검사기-강제
  라즘을 (ii)의 본체로.
- **(rev.2) 비용 특성화를 기여로 승격**. SCC가 0이어도 논문이 성립하는 구조를 확보.
- **(rev.2) 검사기 이름 사용 최소화**: 특정 엔진의 이름은 §5 재현성 한 문장과 artifact
  안내에만 등장. 근거와 예외는 부록 B.
- **(rev.3 신규) §2 novelty를 "합류성을 아무도 안 본다"에서 "다섯 가정 밖"으로 교체.**
  실험 ⑪이 rev.2 주장을 반증했다. ITP 함수 패키지는 **반례가 아니라 문제 중요성의 근거**로
  인용한다. 이 교체는 CAV Application 트랙 루브릭("기존 정리의 실제 적용")에 오히려 더
  맞는다.
- **(rev.3 신규) 비용 비예측성의 1차 증거를 "동일 202규칙 쌍"으로 교체.** 규칙 수를 통제한
  대조라 "경계 효과" 반론이 불가능하다. 44초 vs 4,080초는 보조 증거로 내린다.
- **(rev.3 신규) 예산 혼재를 freeze 재측정의 명시 사유로 등록.** 현재 TIMEOUT 13건은 예산이
  2,040~4,090초로 제각각이라 서로 비교 불가다. §5에서 TIMEOUT을 판정으로 쓰려면 단일 예산이
  전제다.
- **(rev.3 신규) SCC 발견 축의 성격 변경**: "결함 사냥"이 아니라 "503심볼 전체성의 기계 증명
  + 근사 부채의 정량화". `dom:narrow` 0건은 null result가 아니라 보고 가능한 양성 결과다.

---

## 9. 리스크

| 리스크 | 상태 | 대응 |
|---|---|---|
| **⑪ 결과가 나쁨** → ITP 계열 어딘가가 겹치는 절의 합류성을 실제로 검사 | **실현됨** (Isabelle `function` / HOL Light) | **대응 완료** — §2.2를 다섯 축(3-CTRS 자유변수 / 비생성자 헤드 / 비좌선형 / order-sorted / 결정절차·규모)으로 좁혔다. 카탈로그 + 규모 + 비용은 무영향. 잔여: compatibility 의무가 가드를 포함하는지 확정(9월) |
| **⑨ 결과가 나쁨** → `== false` 근사가 측정 슬라이스의 큰 비율을 덮음 | 미측정 | 합류성 주장을 "근사-free 슬라이스 N개"로 재스코프. N이 작으면 **합류성이 헤드라인에서 내려가고** 종료성 + 비용 특성화가 논문의 중심이 된다. **8월 초에 해야 하는 이유**. rev.3: SCC `dom:Val-wide` 405건이 같은 원인이므로 비율이 클 가능성이 오히려 높아졌다 |
| **합류성 커버리지가 안 오름** | 진행 중 (169/565) | 7.1 층화 프로토콜 + §6 selection bias 명시. **silent cap 금지, 그러나 "전수 하겠다"도 금지.** 관측된 편향 방향(874~885 전부 예산 소진)까지 적어 낙관 편향을 선점 |
| **표가 계속 움직임** — 스윕이 진행 중이라 rev.2→rev.3 사이 TIMEOUT 7→13 | 상시 | §1·§5의 모든 수치에 **측정 커밋 해시**를 붙이고, freeze(⑧) 전 수치는 잠정으로 표시. 계획서 개정 때마다 부록 A 체크리스트 재실행 |
| **예산 혼재로 TIMEOUT 비교 불가** | 실현됨 | ⑧ freeze에서 단일 예산 재측정. 그 전까지 §5에 TIMEOUT 간 비교 문장을 쓰지 않는다 |
| **SCC 발견 0** | **부분 실현** (`subty_`/`match_`에서 `dom:narrow` 0건) | 성격 전환 — "503심볼 전체성의 기계 증명"으로 양성 보고. 잔여 가족(함수 435 등)에서도 0이면 비용 특성화가 발견 축 단독 지지(기여 (iii)) |
| **YES\* WLL 검사 내장 미완** | 미완 | freeze까지 못 하면 YES\* 6건을 헤드라인에서 빼고 부록으로. 6건이라 손실 작음 |
| **두 번째 명세 스파이크 실패** | 미착수 | "general" 삭제, 카탈로그 프레이밍 유지. 실패 모드 둘 구분: upstream IL 델타(도구 이전 문제 → §3으로 흡수) vs 관계 모드화 불가(fragment 밖 → R2의 실증적 한계로 §6) |
| **④ ablation 예산 초과** | 미착수 | 층화 표본 + 예산 사전 계산. 계산 결과가 감당 불가면 패스 서브셋(가장 라즘적인 3개)만 |
| **이중맹검 위반** | 중심화된 위험 | 부록 C. 방언 명칭·인터프리터 명칭·"우리 선행 연구" 표현이 파일 아티팩트보다 위험 |

---

## 부록 A — 수치 검증 체크리스트 (freeze 전 정리)

**논문 수치는 verification.md에서 오므로 원본을 먼저 고쳐야 한다.**
아래는 tip `eda41eed`에서 재추출한 결과(2026-07-27).

### A.1 rev.2에서 정정된 수치

| 항목 | rev.2 | tip `eda41eed` 실측 |
|---|---|---|
| 합류성 측정 심볼 | 163 (28.8%) | **169** (29.9%) |
| 합류성 TIMEOUT | 7 | **13** |
| 합류성 미측정 | 402 | **396** |
| 미측정 슬라이스 최소 규칙 수 | "≥874" | **784** |
| "TIMEOUT 7건 전부 비트 조작" | 참으로 서술 | **거짓** — 비트 7 + 리스트/식별자 6(874~885규칙) |

### A.2 확인된(변경 없는) 수치

- 합류성 최대 검사 성공 슬라이스 = `$flatten_annotationList` **868규칙, 1,976.0초** ✓
  (바로 위 874규칙부터 6개가 전부 TIMEOUT이므로 868이 실측 천장)
- 종료성 최대 검사 성공 = `Program_ok` **60,723규칙** (YES, ≥20,480초 — **상한**) ✓
- 종료성 유일 비-YES = `Program_inst` **61,345규칙** TIMEOUT (>20,480초) ✓
- YES\* 6건 = `$write_value_from_bits`(274) + `$write_*_prime` 4(271) +
  `$expression_as_lvalue`(766, 3,198.2초) ✓
- 285규칙 `$bitacc_offset_op` TIMEOUT (>4,090초) ✓
- ~200규칙 구간 스프레드: 200규칙 44.0초 ~ 201규칙 >4,080초 = **92.7배** ✓
- **신규 활용**: 동일 202규칙 `$bin_band` 142.9초 vs `$bin_bor` 1,792.7초 = **12.5배**

### A.3 남은 체크 항목

1. **`Program_ok` "YES (≥20480s)"** → 이 초는 비용이 아니라 rung이다. §5에서 "20,480초에
   검사 성공"으로 읽히면 안 된다. 표기 규약을 캡션에 명시하거나, 7.1의 고정 예산으로
   재측정해 상한 표기를 없앤다.
2. **예산 혼재 정리** — 현재 TIMEOUT 13건의 예산은 2,040 / 2,055~2,058 / 4,080~4,090초로
   제각각. freeze 재측정 전까지 TIMEOUT 간 비교 문장 금지.
3. **CORE_LOGIC.md가 파일 트리와 어긋남** — 문서는 일부 모듈을 "복구·배선 완료"라 하는데
   경로가 이동했다(백엔드가 서브디렉터리로). freeze 전에 문서/트리 동기화. artifact
   심사에서 드러난다.
4. **실험 ①④⑤이 전제하는 CLI 플래그**(`--ctrs-mode`, `--emit-ctrs`, `--disable-pass`,
   `--emit-cops`)가 freeze 커밋에 **실제로 존재하고 동작하는지** 체크리스트로 확인.
   재현 커맨드를 논문에 쓰기 전에.
5. **표의 잠정성 캡션** — 합류성 스윕이 심볼당 프로세스로 진행 중이다. §5의 모든 수치에
   측정 커밋 해시를 붙인다.
6. **⚠️ 실험 ⑤의 심볼 이름 확인** — rev.2는 "무sort 도구가 `$write_value_from_bits`를 YES로
   닫았다(우리는 TIMEOUT)"고 적었으나, 표에서 `$write_value_from_bits`(274규칙)는 **YES\*
   261.2초**이고 TIMEOUT인 것은 이름이 뒤집힌 `$write_bits_from_value`(103규칙, >2,040초)다.
   **둘 중 어느 심볼이 baseline에서 닫혔는지 파일럿 로그로 확정해야 한다.** "예산 초과 7건의
   백업 후보"라는 §5 문장이 여기에 걸려 있다.
7. **문헌 스케일 천장 수치의 출처 확정** — T1의 대비 행 중 두 개가 아직 1차 확인 전이다:
   (a) JavaFAN Java 의미론의 "수백 등식 + confluence를 전제로 가정" 서술, (b) COPS/CoCo
   CTRS 부문 문제의 **규칙 수** 규모("수~수십"). 후자는 문제 **개수**(2023년 161개)만 확인됐고
   문제당 규칙 수는 미확인이다. 확정 전까지 T1에 구체 숫자를 쓰지 않는다.
   (rev.2의 "최대 대회 인증서 ~34 critical peaks"는 출처 미확인이라 rev.3에서 삭제했다.)

---

## 부록 B — 검사기 이름 사용: 지우는 것과 남기는 것

**방침**: 논문은 특정 재작성 엔진에 관한 논문이 아니다. 도구 이름은 재현성에 필요한
최소한으로 줄이고, 도구 계보·역사·정치는 전부 삭제한다.

**삭제 (rev.1에 있던 것)**
- 기존 변환 도구의 역사·번들 구성 변화 서술 → §4 카탈로그 한 행의 **인코딩 진단**으로만
  압축(도구 이름 없이 "기성 변환기의 동등성-검사 인코딩"으로 지칭 가능).
- 실행 프레임워크의 엔진 계보 서술 → §7-2는 **설계 선택 대비**만(CR을 검사하지 않고
  회피한다 / equation-rule 분류가 수동이다). 엔진 이름은 인용으로만 등장.
- proof-score 전통은 본문 문단 → **각주 한 줄**.
- 특정 엔진의 네이티브 지원 유무, 재구현 부재, 버전 이력 같은 도구 정치 → 전부 삭제.
- 결정 기록의 "도구 지형 조사" 항목 → 삭제(부록 A.3의 체크리스트로 대체).

**남기는 것 (3곳, 그리고 그 이유)**
1. **§5 재현성 한 문장** — 어떤 검사기·어떤 버전·어떤 예산으로 측정했는지는 밝혀야 한다.
   한 문장 + artifact 포인터.
2. **§3 검사기 독립성 한 줄** — 백엔드가 격리돼 있어 방법이 검사기 특이적이지 않다는 라거.
   이건 이름을 지우면 오히려 약해지는 곳이다.
3. **실험 ⑤의 sort-erasure penalty** — 이건 도구 홍보가 아니라 **결과**다: 13규칙 함수를
   sorted 검사기는 0.7초에 닫는데 무sort 도구 4개가 전부 실패한다. §4 "단일 sort" 행과
   §2.2 축 4의 유일한 정량 근거다.
   **다만 서술의 주어를 바꾼다**: "이 엔진이 우수하다"가 아니라
   **"order-sorted + 조건부를 동시에 받는 검사기가 사실상 하나뿐이라는 것이 이 분야의
   도구 공백이고, 우리 규모 실측이 그 공백의 비용을 처음 정량화한다."**
   그러면 도구 이야기가 아니라 field-level 관찰이 된다. → 이게 "엔진 이야기 제거"의
   가장 좋은 형태다: 지우는 게 아니라 **주어를 도구에서 문제로 옮기는 것.**

**주의 — 제거에 실제 비용이 나오는 지점**: 경험적 의미 보존 증거(differential testing)는
실행 런타임 같은 엔진 위에 서 있다. §5에서 그 증거를 요약할 때 실행 백엔드를 어떻게
지칭할지 정해야 한다. 권장: "동일 번역에서 생성한 실행 가능 백엔드와 레퍼런스
인터프리터 사이의 differential"로 서술하고 엔진 이름은 재현성 문장에 흡수.

---

## 부록 C — 이중맹검 체크리스트 (⑬)

CFP: 위반은 **리뷰 없이 리젝트**. 파일 아티팩트보다 **산문**이 위험하다.

- [ ] 방언 명칭을 그대로 쓰면 그룹이 식별되는지 검토. 필요하면 "P4 방언" 또는 익명화된
      이름으로 지칭하고, 명칭이 필요한 곳은 각주로.
- [ ] SpecTec OOPSLA'24를 **3인칭**으로. "우리 DSL에 대한 논문", "우리 선행 연구" 금지.
- [ ] 차등 오라클로 쓰는 인터프리터가 같은 그룹의 도구라면 3인칭 + 저장소 링크 제거.
- [ ] 저장소·브랜치·커밋 해시를 본문에 쓰지 않는다. artifact는 익명 저장소로.
- [ ] 감사말·펀딩 절은 제출본에서 제거.
- [ ] 내부 노트 파일, 작업 문서, 커밋 로그가 artifact에 섞이지 않도록 별도 패스.
- [ ] PDF 메타데이터(작성자·제목) 확인.
- [ ] arXiv 업로드는 마감 전 1주 ~ 후 1개월을 피한다(CFP 권고).
- [ ] 제출 시 **artifact 제출 의향** 표시 여부 결정(표시하고 안 내면 설명 요구).

---

## 부록 D — 제목 후보

응용 트랙 루브릭("어떤 정리를, 어떤 문제에")에 맞춘 표현:

1. *Gating Well-Definedness of a Production Language Specification: Confluence and
   Termination Checking at 60k Rules*
2. *Are Mechanized Semantics Well-Defined? Applying Confluence and Termination Checkers
   to a Real P4 Specification*
3. *What Breaks When You Point a Confluence Checker at 60,000 Rules*

(3)이 2단계 리뷰의 1라운드에 가장 유리하다 — 결과가 제목에 있다. (1)은 규모 수치를
제목에 넣으므로 §1 표와 반드시 일치해야 한다(축별 분리 주의).

**rev.3 주의**: 어느 후보든 "first"/"novel" 어휘를 제목에 넣지 않는다. §2가 좁아진 이상
제목이 §2보다 넓은 주장을 하면 안 된다.
