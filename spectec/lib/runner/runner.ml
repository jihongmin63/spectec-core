(** Runner - Test infrastructure.

    Provides test suite runners and checkpoint persistence. Core types (Error,
    Task, Target) and pipeline functions live in the {!Spectec} facade. *)

module Suite = Suite
module Checkpoint = Checkpoint
